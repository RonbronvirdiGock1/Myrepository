base = float(input("Enter the base number (e.g., 2): "))
exponent = float(input("Enter the exponent (e.g., 3): "))

result = base ** exponent

print(f"\n{base} raised to the power of {exponent} is: {result}")